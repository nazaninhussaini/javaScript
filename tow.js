const number = prompt("یک عدد را وارد کنید تا جدول ضرب زبانی انرا برایتان بسازم");
for(let m=1; m<=12; m++){
    console.log(m +"*" + number + "=" +m*number);
}
const name ="nazanin"
const lastName = "hussaini"
console.log("Hi my name is " + name + " " +lastName);
const add = prompt("یک عدد را وارد کنید تا آنرا برایتان به توان بالا ببرم");
for(let n=1; n<=6; n++){
    console.log(n +"**" + add + "=" +n**add);
}
